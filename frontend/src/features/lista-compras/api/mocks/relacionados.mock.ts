export const mockRelacionados: Record<number, number[]> = {
    2: [3],     // Café → Filtro de Papel
    4: [5],     // Arroz → Feijão
    1: [6, 2],  // Leite → Pão Integral, Café
};

export const mockPopulares: number[] = [4, 5, 1, 2, 6];
